// Copy this file to assets/config.js and fill in your Supabase project values.
// assets/config.js is gitignored — never commit real keys through it directly,
// though the anon key is safe for client-side exposure by Supabase's design.
window.APP_CONFIG = {
  SUPABASE_URL: 'https://YOUR-PROJECT.supabase.co',
  SUPABASE_ANON_KEY: 'YOUR-ANON-KEY'
};
